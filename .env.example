# zhanwen-admin（仓库根目录）.env 仅用于 docker-compose.yml 读取
# 本地开发（非 Docker）请改：backend/.env（复制 backend/.env.example）

# Docker / docker-compose
POSTGRES_PASSWORD=your-db-password
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
ENCRYPTION_KEY=your-32-character-encryption-key-here

# 可选：后端模型 Provider Key（不用可留空）
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
DEEPSEEK_API_KEY=
